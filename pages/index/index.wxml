<view class="container">
  <van-button type="primary" bindtap="handleLogin" wx:if="{{!isLoggedIn}}">微信授权登录</van-button>
  <view wx:elif="{{isLoggedIn}}">
    <text>欢迎，{{userInfo.nickName}}</text>
    <van-button type="primary" bindtap="navigateTo" data-page="family">家庭管理</van-button>
    <van-button type="primary" bindtap="navigateTo" data-page="items">物品管理</van-button>
    <van-button type="primary" bindtap="navigateTo" data-page="orders">点单</van-button>
  </view>
</view>
